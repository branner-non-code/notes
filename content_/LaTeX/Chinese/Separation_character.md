Separation character between Chinese and English

`\CJKsetecglue` command sets the separation character between Chinese and other blocks of text.

Default is `\CJKsetecglue{ }`. To suppress it, use `\CJKsetecglue{}`.

