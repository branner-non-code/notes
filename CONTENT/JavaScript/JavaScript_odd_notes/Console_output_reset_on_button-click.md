### Console-output is reset almost immediately on button-click.

 This is default behavior. It must be stopped manually with the `preventDefault()` or `stopPropagation()` methods, called on the Event object.

[end]
